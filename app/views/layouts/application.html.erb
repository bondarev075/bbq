<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bbq" %></title>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
    <script src="https://api-maps.yandex.ru/2.1/?apikey=84772dfa-c33f-4fc6-bdee-67c122ba3509&lang=ru_RU" type="text/javascript"></script>
    <%= stylesheet_pack_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to t('links.profile'), current_user, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to t('links.sign_out'), destroy_user_session_path, method: :delete, class: "nav-link" %></li>
            <% else %>
              <li class="nav-item"><%= link_to t('links.sign_in'), new_user_session_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to t('links.register'), new_user_registration_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <% flash.each do |type, msg| %>
            <div class="alert alert-danger buffer-top">
              <%= msg %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </div>
      <footer>
        <p>&copy; <%= t 'app.company' %> 2020</p>
      </footer>
    </div>
  </body>
</html>
