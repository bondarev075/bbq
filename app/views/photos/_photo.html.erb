<span class="event-photo buffer-both buffer-left">
  <% image_title = "Добавил #{render partial: 'users/user', object: photo.user}" %>
  <%= link_to image_tag(photo.photo.thumb.url),
              photo.photo.url,
              data: {
                  lightbox: "event-photos",
                  title: image_title
              }
  %>
</span>
